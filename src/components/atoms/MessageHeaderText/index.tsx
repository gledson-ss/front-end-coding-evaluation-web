import React from 'react';

import * as Styled from './styles';

const MessageHeaderText: React.FC = () => (
  <Styled.MessageHeaderText>
    Contact List.
  </Styled.MessageHeaderText>
);

export default MessageHeaderText;
