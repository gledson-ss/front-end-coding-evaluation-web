import styled from 'styled-components';
import { RiContactsFill } from 'react-icons/ri';

export const ContactPageIcon = styled(RiContactsFill)`
  color: ${(props) => props.theme.colors.orange};
  width: 80px;
  height: 80px;
`;
