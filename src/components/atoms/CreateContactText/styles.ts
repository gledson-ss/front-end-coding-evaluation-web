import styled from 'styled-components';

export const CreateContactText = styled.p`
  font-size: 16px;
  color: ${(props) => props.theme.colors.white};
  font-family: 'Poppins', sans-serif;
  font-weight: 600;
`;
