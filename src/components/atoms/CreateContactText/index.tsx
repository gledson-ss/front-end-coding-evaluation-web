import React from 'react';

import * as Styled from './styles';

const CreateContactText: React.FC = () => (
  <Styled.CreateContactText>
    Add Contact
  </Styled.CreateContactText>
);

export default CreateContactText;
